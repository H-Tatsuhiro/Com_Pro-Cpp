#include <iostream>
#include <cmath>
#include <algorithm>
#include <vector>
using namespace std;
int main() {
    int x, a, b; cin >> x >> a >> b;
    cout << x + 1 << endl << (x + 1) * (a + b) << endl << (x + 1) * (a + b) * (x + 1) * (a + b) << endl << (x + 1) * (a + b) * (x + 1) * (a + b) - 1 << endl;
}
